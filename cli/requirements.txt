# Python Dependencies
adb-shell>=0.4.3
rich>=13.0.0
click>=8.0.0
jinja2>=3.0.0
requests>=2.28.0
pyyaml>=6.0
colorama>=0.4.6
tabulate>=0.9.0

# System Dependencies (installed separately, not via pip)
# - Android SDK Platform Tools (ADB) - https://developer.android.com/studio/releases/platform-tools
# - scrcpy (Screen Mirroring) - https://github.com/Genymobile/scrcpy
#   Windows: scoop install scrcpy
#   macOS: brew install scrcpy
#   Linux: sudo apt install scrcpy
